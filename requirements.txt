# Python Dependencies — Thoughts Dashboard
#
# SECURITY REQUIREMENTS (specs/security/dependency_management.spec):
# - All versions pinned with == (immutable builds)
# - Scan: pip-audit -r requirements.txt
# - SBOM: cyclonedx-py requirements -r requirements.txt -o sbom.json

# Web Framework
Flask==3.0.3

# CSRF Protection (specs/security/web_security.spec REQ-7)
Flask-WTF==1.2.1

# Database
psycopg2-binary==2.9.9

# Env var loading
python-dotenv==1.0.1

# Production WSGI Server
gunicorn==22.0.0

# Security & Compliance tooling
pip-audit==2.7.3
bandit==1.7.9
detect-secrets==1.4.0

# Testing
pytest==8.2.2
pytest-cov==5.0.0
